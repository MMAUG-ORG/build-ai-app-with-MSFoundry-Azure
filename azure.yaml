# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: mmaug-foundry-support-app
metadata:
  template: mmaug-foundry-support-app@1.0.0
services:
  api:
    project: ./backend
    language: python
    host: appservice
  web:
    project: ./frontend
    language: js
    host: appservice
    dist: dist
hooks:
  postprovision:
    shell: sh
    run: |
      PY=$(command -v python3 || command -v python)
      "$PY" -m pip install --quiet --user azure-ai-projects azure-ai-agents azure-identity
      "$PY" ./scripts/create_agent.py
    interactive: false
    continueOnError: false
